import { ArticleLayout } from '@/components/ArticleLayout'

export const article = {
  author: 'Nicolas Dabreo',
  date: '2024-07-16',
  title: 'Themeing Phoenix with TailwindCSS',
  description:
    'Looking to implement dark mode or configurable themes to your Phoenix app? Look no further.',
  published: true
}

export const metadata = {
  title: article.title,
  description: article.description,
}

export default (props) => <ArticleLayout article={article} {...props} />

Sorry, this is a placeholder for now, thanks for looking!

## Lorem

Code example

```elixir
defmodule Example do
  def hello(), do: :world
end
```
